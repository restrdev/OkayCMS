<?php

/**
 * Бутстрап тестов.
 *
 * Константы подключаются здесь, а не в конструкторах тест-кейсов: data-провайдеры
 * объявлены статическими (этого требует PHPUnit 11) и вызываются до создания
 * экземпляра тест-класса, поэтому к моменту их вызова константы уже должны существовать.
 */

require_once __DIR__ . '/../vendor/autoload.php';
require_once __DIR__ . '/../Okay/Core/config/constants.php';
